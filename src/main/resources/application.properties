# Project Name
spring.application.name=shikshasetu

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/shikshasetu
spring.datasource.username=postgres
spring.datasource.password=ov@2004

# ? THE FIX IS DONE: Changed back to 'update' to keep your data safe
# CHANGE THIS:
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Security Configuration
# jwt.secret=your_super_secret_key_here
# jwt.expiration=86400000

# Gemini AI Configuration
gemini.api.key=AIzaSyCJfBxdRPevD2pT13BWHss9jeL4WzyUyHM

# Logging for debugging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE